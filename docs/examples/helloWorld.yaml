scripts:
  hello-qdup:
    - sh: echo ${{GREETING}}

hosts:
  local : ${{USER}}@localhost
roles:
  run-hello-qdup:
    hosts:
      - local
    run-scripts:
      - hello-qdup
states:
  GREETING: Hello qDup!
  USER:
  HOST:

